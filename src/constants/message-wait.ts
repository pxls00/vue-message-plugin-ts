import type IMessageItem from '@/interfaces/messages/message-item-base'

const MESSAGE_WAIT_ACTION_CREDENTIALS: IMessageItem = {
  title: 'Wait please ...',
  type: 'wait'
}

export default MESSAGE_WAIT_ACTION_CREDENTIALS